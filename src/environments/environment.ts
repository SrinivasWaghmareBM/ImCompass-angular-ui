export const environment = {
    production: false,
    apiUrl: 'https://localhost:7001/api',   // Change this when deploying
    useMockData: true                       // Set to false when real API is ready
  };